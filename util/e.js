

export const cfgUndefined = "VueTrans requires configuration. Please pass a configuration object that contains, at minimum, your Vuex store under the `store` field. It should be the second argument to `Vue.use` (e.g., `Vue.use(VueTrans, { store: myStore })`).";

export const cfgStoreUndefined = "Your configuration object must include the Vuex store you intend to use (format `{ store: myStore }`).";
